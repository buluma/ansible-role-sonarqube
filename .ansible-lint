---
#
# Ansible managed
#
exclude_paths:
  - meta/preferences.yml
  - molecule
  - .tox
  - .cache
  - .github
  - requirements.yml

skip_list:
  - yaml[truthy]
  - name[template]

enable_list:
  - name[prefix]
